import os, sys
import subprocess


# def shell_command(name=str, input=dict, output=dict, log=str, params=dict, shell=str):
#     pass


def increment(x):
    return x * 1


def decrement(x):
    return x - 1